**实训二（回归问题）**

**要求：给出相关代码及结果分析，可使用****RMarkdown****创建。**

1.     **数据准备：**加载程序包MASS中的Boston数据集

1)      通过网络搜索Boston数据集的介绍；

2)      检测是否有缺失值

3)      调用GGally包中的ggpairs函数绘制成对关系图

4)      设crime为因变量，通过图表描述你的发现，与哪些变量相关程度较高。

5)      随机选取70%数据作为训练集，30%作为测试集。（固定种子）

2.     基于训练集数据，建立crime对所有自变量的多元线性回归模型，并进行模型诊断，对结果进行解释。输出回归系数，并基于测试集数据，输出预测误差MSE、MAE。

3.     基于训练集数据，使用step函数对多元线性回归模型，进行逐步回归（基于AIC、BIC准则选择），对结果进行解释。输出回归系数，并基于测试集数据，输出预测误差MSE、MAE。

4.     基于训练集数据，利用glmnet包，进行岭回归，通过10折CV（固定种子），选择最优调节参数lambda，进一步利用最优调节参数进行估计，并对回归结果进行分析。输出回归系数，并基于测试集数据，输出预测误差MSE、MAE。

5.     基于训练集数据，利用glmnet包，进行Lasso回归，通过10折CV（固定种子），选择最优调节参数lambda，进一步利用最优调节参数进行估计，并对回归结果进行分析。输出回归系数，并基于测试集数据，输出预测误差MSE、MAE。

6.     基于训练集数据，利用glmnet包，使用弹性网络回归方法（令alpha=0.5），通过10折CV（固定种子），选择最优参数lambda。进一步，使用for循环，令alpha=0,0.1,0.2，…，0.9,1，并通过10折CV（固定种子），选择弹性网络回归的最优参数alpha。最后利用最优调节参数进行估计，并对回归结果进行分析。输出回归系数，并基于测试集数据，输出预测误差MSE、MAE。

7.     基于训练集数据，利用ncvreg包，使用SCAD方法，通过10折CV（固定种子），选择最优参数lambda，进一步利用最优调节参数进行估计，并对回归结果进行分析。输出回归系数，并基于测试集数据，输出预测误差MSE、MAE。

8.     基于训练集数据，利用msgps包，使用自适应Lasso方法，按照GCV准则选取最优参数lambda，输出回归系数。基于测试集数据，输出预测误差MSE、MAE。

9.     通过表格汇总展示2-8步的预测误差、回归系数，并进行对比分析。